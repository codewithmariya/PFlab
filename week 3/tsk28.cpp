#include <iostream>
using namespace std;
 main() {
    int sum = 0;   
    int input;  

    cout << "Enter 5 integers, one by one:" << endl;

    
    cin >> input;
    sum = sum + input;

    
    cin >> input;
    sum = sum + input;

   
    cin >> input;
    sum = sum + input;

    
    cin >> input;
    sum = sum + input;

    
    cin >> input;
    sum = sum + input;

    cout << "The total sum is: " << sum << endl;

    
}